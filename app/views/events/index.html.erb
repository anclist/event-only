<%= link_to "Create Event", new_event_path %>

<h1>All the events</h1>

<ul>
  <% @events.each do |event| %>
  <!-- <%= event.inspect %> <br> -->

  <li><%= event.name %>, <%= event.date.strftime("%A, %h %d, %Y") %> <%= link_to "details", event_path(event) %></li>
  <% end %>
</ul>
